const response = require('../../response');
const config = require('../../config');
const _ = require('lodash')
var VipPost = require('../../model/vipPst');

module.exports = {
    GET_VIP_USER: (req, res, next) => {
        var andList = [];
        var search = [];
        var limit = 1000;
        var skip = 0;

        console.log('test');
    }
}